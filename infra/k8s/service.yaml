apiVersion: v1
kind: Service
metadata:
  name: docs
  namespace: erp-dev
  labels:
    app.kubernetes.io/name: docs
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: docs
  ports:
    - name: http
      port: 80
      targetPort: http
